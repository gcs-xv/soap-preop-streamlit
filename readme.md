# SOAP Terjaring → SOAP Pre-Op (Streamlit)

App untuk mengubah SOAP mentah (rawat jalan / non pre-op) menjadi SOAP Pre-Op otomatis (tanggal operasi H+1).

## Run lokal
```bash
pip install -r requirements.txt
streamlit run app.py
